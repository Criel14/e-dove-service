<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.criel.edove.user.mapper.UserMapper">

    <select id="getRolesByUserId" resultType="com.criel.edove.user.entity.Role">
        SELECT r.*
        FROM role r
                 INNER JOIN user_role ur ON r.role_id = ur.role_id
        WHERE ur.user_id = #{userId}
    </select>

    <select id="getPermissionsByUserId" resultType="com.criel.edove.user.entity.Permission">
        SELECT DISTINCT p.*
        FROM permission p
                 INNER JOIN role_permission rp ON p.permission_id = rp.permission_id
                 INNER JOIN user_role ur ON rp.role_id = ur.role_id
        WHERE ur.user_id = #{userId}
    </select>

</mapper>
