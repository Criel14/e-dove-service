<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.criel.edove.auth.mapper.UserAuthMapper">

    <select id="getRolesByUserId" resultType="com.criel.edove.auth.entity.Role">
        SELECT r.*
        FROM user_auth u
                 INNER JOIN user_role ur ON u.user_id = ur.user_id
                 INNER JOIN role r ON ur.role_id = r.role_id
        WHERE u.user_id = #{userId}
    </select>

    <select id="getPermissionsByUserId" resultType="com.criel.edove.auth.entity.Permission">
        SELECT p.*
        FROM user_auth u
                 INNER JOIN user_role ur ON u.user_id = ur.user_id
                 INNER JOIN role r ON ur.role_id = r.role_id
                 INNER JOIN role_permission rp ON r.role_id = rp.role_id
                 INNER JOIN permission p ON rp.permission_id = p.permission_id
        WHERE u.user_id = #{userId}
    </select>
</mapper>
