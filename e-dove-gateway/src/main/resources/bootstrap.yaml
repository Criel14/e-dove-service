spring:
  application:
    name: e-dove-gateway

  cloud:
    # nacos 配置
    nacos:
      discovery:
        server-addr: ${E_DOVE_DOCKER_IP_ADDR}
        namespace: e-dove-1014
        group: EDOVE_GROUP
        username: nacos
        password: nacos
