spring:
  application:
    name: e-dove-assistant

  cloud:
    # nacos 配置
    nacos:
      discovery:
        server-addr: ${E_DOVE_DOCKER_IP_ADDR}
        namespace: e-dove-1014
        group: EDOVE_GROUP
        username: nacos
        password: nacos
      config:
        server-addr: ${E_DOVE_DOCKER_IP_ADDR}
        namespace: e-dove-1014
        file-extension: properties
        group: EDOVE_GROUP
        username: nacos
        password: nacos